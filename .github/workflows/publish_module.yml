name: 模块发布

on:
  push:
    paths:
      - 'gradle.properties'
    branches:
      - master

jobs:
  generate-props:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4